 - 'api/bookings/hydrated' - get заппрос который возвращает все бронирования
query params: {
    filters: {
        start: "iso date",
        end: "iso date",
        entryId: "string",
    }
}

type: {
  select: {
    id: true,
    number: true,
    start: true,
    end: true,
  },
}

 - 'api/settings' есть query parametr seasons для галлереи